import { ExternalLink, Film, Image, Scissors, Type } from 'lucide-react';
import type { Project, Scene } from '../lib/supabase';

export default function EditorHandoff({ project, scenes, onOpen }: { project: Project; scenes: Scene[]; onOpen: () => void }) {
  return <div className="mx-auto max-w-5xl px-6 py-10"><p className="text-xs font-bold uppercase tracking-[.2em] text-[var(--space-text-brand)]">Step 8 · Editor handoff</p><h1 className="mt-2 text-3xl font-bold text-[var(--space-text-primary)]">Fine-tune without flattening the timeline</h1><p className="mt-2 text-[var(--space-text-secondary)]">Each supporting scene is handed off as a separate editable item. Avatar sections keep their original audio.</p>{project.assembled_video_url ? <video src={project.assembled_video_url} controls className="mt-6 max-h-[520px] max-w-full rounded-2xl bg-black" /> : null}<div className="mt-6 grid gap-3 sm:grid-cols-2">{scenes.map((scene) => <div key={scene.id} className="rounded-xl border border-[var(--space-border-default)] bg-[var(--space-surface-card)] p-4"><div className="flex items-center gap-2 font-semibold text-[var(--space-text-primary)]"><Film className="h-4 w-4 text-[var(--space-text-brand)]" />Scene {scene.scene_index}</div><p className="mt-1 text-sm text-[var(--space-text-secondary)]">{Number(scene.script_start_sec).toFixed(1)}–{Number(scene.script_end_sec).toFixed(1)}s · {scene.scene_type}</p><div className="mt-3 flex flex-wrap gap-3 text-xs text-[var(--space-text-muted)]"><span><Image className="mr-1 inline h-3 w-3" />swap or regenerate</span><span><Scissors className="mr-1 inline h-3 w-3" />trim or remove</span><span><Type className="mr-1 inline h-3 w-3" />text or logo</span></div></div>)}</div><button onClick={onOpen} className="mt-6 flex items-center gap-2 rounded-xl bg-[var(--space-brand-primary-600)] px-5 py-3 font-semibold text-white"><ExternalLink className="h-4 w-4" />Open SceneForge timeline in Editor</button></div>;
}
