import { Check } from 'lucide-react';
import { STYLE_REGISTRY } from '../styles/registry';

export default function StylePicker({ value, onChange, onConfirm }: { value?: string; onChange: (id: string) => void; onConfirm: () => void }) {
  return <div className="mx-auto max-w-6xl px-6 py-9"><p className="text-xs font-bold uppercase tracking-[.2em] text-[var(--space-text-brand)]">Step 4 · Visual language</p><h1 className="mt-2 text-3xl font-bold text-[var(--space-text-primary)]">Choose one world for every scene</h1><p className="mt-2 text-[var(--space-text-secondary)]">Each style controls image prompts, transitions, easing, typography and palette.</p><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{STYLE_REGISTRY.map((style) => { const selected = value === style.id; return <button key={style.id} onClick={() => onChange(style.id)} className={`overflow-hidden rounded-2xl border text-left transition ${selected ? 'border-[var(--space-brand-primary-500)] ring-2 ring-[color-mix(in_srgb,var(--space-brand-primary-500)_30%,transparent)]' : 'border-[var(--space-border-default)]'}`}><div className="h-28" style={{ background: style.preview }} /><div className="bg-[var(--space-surface-card)] p-4"><div className="flex items-center justify-between font-semibold text-[var(--space-text-primary)]"><span>{style.name}</span>{selected ? <Check className="h-4 w-4 text-[var(--space-text-brand)]" /> : null}</div><p className="mt-1 text-sm leading-5 text-[var(--space-text-secondary)]">{style.description}</p><div className="mt-3 flex gap-1">{style.palette.slice(0,5).map((color) => <span key={color} className="h-4 w-4 rounded-full border border-white/10" style={{ background: color }} />)}</div></div></button>; })}</div><button disabled={!value} onClick={onConfirm} className="mt-7 rounded-xl bg-[var(--space-brand-primary-600)] px-5 py-3 font-semibold text-white disabled:opacity-40">Confirm style</button></div>;
}
