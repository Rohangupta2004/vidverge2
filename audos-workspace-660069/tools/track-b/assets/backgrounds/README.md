# backgrounds/
Background images frozen for reuse. Format: PNG/WebP. Grain/noise is NEVER an image asset — compositions use inline CSS `radial-gradient` only (SVG filters and data:image/svg+xml taint html2canvas).
